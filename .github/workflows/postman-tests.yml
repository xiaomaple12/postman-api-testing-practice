name: Postman API Tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  postman-api-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman collection
        run: newman run Postman_API_Testing_Practice.postman_collection.json -e Postman_Echo_Environment.postman_environment.json